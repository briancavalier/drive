# Plan

1. Study the current repair exhaustion flow in `scripts/lib/event-router.mjs`, `scripts/lib/repair-state.mjs`, and `scripts/prepare-review-artifact-repair.mjs` to confirm how `nextRepairState` flags `blocked`, what metadata is available, and where failure interventions are built today.
2. Introduce a helper (e.g., `buildRepairExhaustionQuestion`) under `scripts/lib/` that constructs the standardized question payload, summary, detail text, options, and resume context described in the spec, along with focused unit tests for the helper.
3. Update `routeWorkflowRun` and `routePullRequestReview` in `scripts/lib/event-router.mjs` to call the new helper whenever `repairState.blocked` is true, include the rendered question comment via `renderInterventionQuestionComment`, and keep the existing behavior otherwise.
4. Extend `scripts/route-pr-loop.mjs` and `.github/workflows/factory-pr-loop.yml` so the routed comment output is passed through to `FACTORY_COMMENT`, falling back to the prior fixed string only when the output is empty; make sure `FACTORY_REPAIR_ATTEMPTS`, `FACTORY_BLOCKED_ACTION`, and other env values stay consistent.
5. Modify `scripts/prepare-review-artifact-repair.mjs` so the blocked path uses the shared helper, returns the rendered question comment, and ensure the workflow step that calls `handle-stage-failure` instead posts the question comment when blocking review processing.
6. Expand `scripts/apply-intervention-answer.mjs` (and `scripts/lib/github-messages.mjs` if needed) to understand `questionKind: "repair_exhaustion"`, support the `reset_to_plan_ready` effect, adjust resolution comments, and manage `pendingStageDecision`, pause state, and status transitions per option.
7. Refresh UI-facing helpers in `scripts/lib/github-messages.mjs` and `scripts/lib/control-panel.mjs` to add the new option hint and guarantee the control panel surfaces the question summary when present.
8. Update and add unit coverage across `tests/event-router.test.mjs`, `tests/route-pr-loop.test.mjs`, `tests/prepare-review-artifact-repair.test.mjs`, `tests/apply-intervention-answer.test.mjs`, `tests/control-panel.test.mjs`, and any new helper tests to lock in the new behavior, then run the affected test suites (or full `npm test`).
9. Document the new operator flow in `README.md` (or the most relevant operator guidance section) to explain how the repair exhaustion question works.
