# Plan

1. Inspect the current implement-stage intervention flow across `scripts/detect-stage-intervention-request.mjs`, `scripts/handle-stage-intervention-request.mjs`, `scripts/apply-intervention-answer.mjs`, and `scripts/build-stage-prompt.mjs` to document how `questionKind`, option validation, and `pendingStageDecision` are handled today, and note the relevant tests (`tests/detect-stage-intervention-request.test.mjs`, `tests/handle-stage-intervention-request.test.mjs`, `tests/apply-intervention-answer.test.mjs`, `tests/build-stage-prompt.test.mjs`).
2. Update `scripts/detect-stage-intervention-request.mjs` so validation accepts both ambiguity and new scope/priority tradeoff payloads (ensuring resumable-option counts, instructions, and recommended option rules) and expand the related unit tests to cover accepted and rejected cases.
3. Modify `scripts/handle-stage-intervention-request.mjs` to forward the request’s `questionKind` into the built intervention, keeping resume-context wiring intact; adjust `tests/handle-stage-intervention-request.test.mjs` to assert the new behavior.
4. Generalize `buildPendingStageDecision` in `scripts/apply-intervention-answer.mjs` (and associated helpers/tests) so resumable answers from either question kind persist the correct `pendingStageDecision` shape, including the new `scope_priority` kind.
5. Ensure resumed prompts render the stored tradeoff decision clearly by exercising `scripts/build-stage-prompt.mjs` with `pendingStageDecision.kind === "scope_priority"` (updating `tests/build-stage-prompt.test.mjs`) and by extending PR metadata tests to cover normalization of the new kind.
6. Review stage cleanup paths (e.g., `scripts/process-review.mjs`, workflow contracts) to confirm `pendingStageDecision` is cleared after successful stages or resets, and add/adjust tests so tradeoff decisions do not linger beyond their intended scope; update `.factory/prompts/implement.md` to instruct authors when to emit a scope/priority question versus ambiguity.
