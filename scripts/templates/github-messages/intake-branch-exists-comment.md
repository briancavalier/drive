Factory planning did not start because the existing planning branch `{{BRANCH}}` is still present on origin.

Reuse or clean up the previous planning branch or PR, then comment `{{RETRY_COMMAND}}` again.
